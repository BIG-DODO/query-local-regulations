# 小程序集成约定（新工具开发必读）

> 目的：第二个工具网页版开发完成后，内核可**零改动**集成进微信小程序（未来园产业咨询工具集）。
> 本文与 K3_HANDOVER.md、MEMORY.md 配套；由 2026-07 指标测算工具小程序化（0.1→0.3）的全部踩坑沉淀而来。

## 一、计算内核与 UI 分离（最重要，决定集成是"几天"还是"几周"）

**目标**：内核 JS 文件将来原封不动拷进小程序 `miniprogram/lib/` 直接 `require`，一行不改。

规则：

1. 内核文件里**禁止出现**：`document`、`window`、`alert`、`localStorage`/`sessionStorage`、`fetch`/`XMLHttpRequest`、`navigator`、`location`——小程序运行时这些全都不存在。
2. 也**禁止 Node 内置模块**（`fs`/`crypto`/`stream`/`Buffer`）——浏览器和小程序都没有；计算全程内存完成。
3. 实在绕不开的全局引用，必须带守卫：`typeof window !== 'undefined' && window.xxx`。
4. 内核只暴露**纯函数**：入参 plain object → 返回 plain object，不读写外部状态。错误用 `{ error: '...' }` 返回，**不要 throw、不要 alert**（页面层决定怎么提示）。
5. 文件尾做双端导出，模板：

```js
(function (global) {
  'use strict';
  const NS = {};
  global.MyTool = NS;
  // ... 全部函数挂在 NS 上 ...
})(typeof window !== 'undefined' ? window : (typeof globalThis !== 'undefined' ? globalThis : this));

if (typeof module !== 'undefined' && module.exports) {
  const _g = typeof window !== 'undefined' ? window : globalThis;
  module.exports = _g.MyTool;
}
```

6. 语法用 ES6 以内、CommonJS 风格（`require`/`module.exports`），**不要用 ES Module 的 `import`/`export`**——小程序编译器对 CJS 兼容性最稳。

## 二、Excel 导出口径

1. 库**锁定 `xlsx-js-style@1.2.0`**（jsdelivr CDN）——带样式支持（黄色输入格等），官方 SheetJS 没有样式，换库导出的表格会丢全部格式。
2. 每个下载函数的结构必须是：构造 workbook → **最后只调一次 `XLSX.writeFile(wb, fileName)`**。小程序端靠拦截这一个调用拿到工作簿转微信文件系统；多调一次或换别的方式（`writeFileAsync`、a 标签下载、FileSaver）集成时都要返工。
3. 单元格约定（与现有工具一致，将来总表集成才不打架）：
   - 数字格 `t='n'`；带公式的格子必须写 JS 预算的缓存值 `v`（打开文件未重算时也有数）；
   - 所有 SUM 及中间公式包 `ROUND(...,2)`；
   - SUMIF/COUNTIF 的文本条件写**公式内字面量**（`"分栋厂房"`），禁止引用标签格；
   - 黄色可编辑输入格底色 `FFF2CC`；
   - 全表 `wrapText: false`。
4. **每个 sheet 设显式行高**（按行内最大字号 ×1.6、下限 18pt）——不设的话手机微信/网页预览会把某些行渲染成"巨高行"。
5. 合并单元格属性名是 `ws['!merges']`——**有 s**！写成 `'!merge'` 不报错但静默失效。
6. sheet 名 ≤31 字符、定下来就别改（跨表公式按名字引用）；中文名 OK。

## 三、数据流与业务口径

1. 页面/步骤之间传数据**只用 plain object**（内存变量或全局对象），不要用 sessionStorage 当数据总线——小程序没有，现有小程序用 `App.globalData` 串联。
2. 单位口径（跟现有工具对齐）：金额**万元**保留两位小数、面积 **㎡**、比率**按百分比存**（`55` 表示 55%）、年租金 = 日租金 × 360。
3. 所有"百分比 → 小数"的转换只发生在**调用内核的边界处一次**，并在代码注释里写明转换点。（教训：投资估算 inputs 是网页端先 /100，静态/动态 inputs 是内核内部 /100，两套并存极易搞混。）
4. 产品类型等中文枚举值一旦定了就别改——Excel 跨表公式、静态/动态联动都按字面量匹配。

## 四、其他注意事项

1. **从第一天就写 Node 自检脚本**：参照 `test-financial-selfcheck.js` 的模式（vm 沙箱 mock window/XLSX → 调内核 → 断言数值和公式结构）。要求：改内核必跑、新功能同步补断言。这是集成时"两端同参同果"的底气。
2. **第三方库先审后用**：引入前验证能在无 DOM 环境跑（Node 里 require 不炸是底线）。体积也要看——小程序单包上限 2MB，ECharts 这种 1MB 的大家伙一个工具最多一份。
3. **不要引入构建工具**（webpack/vite/npm 依赖链）——网页版纯 HTML+CDN、小程序直接 require 源码，保持零构建结构。
4. **网页端缓存版本号**：HTML 里引用 JS 带 `?v=日期-序号`，每次改 JS 必须递增（GitHub Pages + CDN + 浏览器三层缓存，"推了≠用户端生效"）。
5. **Git 纪律**：只推约定远端；commit message 先确认再提交。
6. **为集成预留接口**：如果计算结果将来可能进跨工具报表（如"指标测算总表"），提前把结果对象顶层结构（输入区/指标区/明细区）设计清晰，集成时按块引用即可。
7. 常备一份**样例数据 + 预期结果**（如自检里的 40000㎡/上海/容积率 2.0 样例），集成联调时拿它做对拍基准。

---

*2026-07-30 整理。集成实施时联系本会话（读 K3_HANDOVER.md + MEMORY.md 恢复上下文）。*
